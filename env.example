# Backend API URL
# For local development:
VITE_API_URL=http://localhost:5000

# For production deployment (replace with your actual backend URL):
# VITE_API_URL=https://your-backend-domain.com
# Example: VITE_API_URL=https://api.ghumakkars.in

# IMPORTANT: After updating this file, rename it to .env (remove .example)
# Make sure to set this environment variable in your Vercel deployment settings
# Vercel > Your Project > Settings > Environment Variables > Add VITE_API_URL
